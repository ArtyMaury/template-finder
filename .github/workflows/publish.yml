name: CI

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install
      run: npm i
    - name: publish
      run: vsce publish -p ${{ secrets.VSCE_TOKEN }}
